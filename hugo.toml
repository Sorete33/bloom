baseURL = 'http://localhost/'
locale = 'es-AR'
title = 'In Bloom | Arte'

[taxonomies]
  category = 'categories'
  tag = 'tags'

[markup]
  [markup.goldmark]
    [markup.goldmark.renderer]
      unsafe = true
